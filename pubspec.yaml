name: build_post_man_collection
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.0-134.0.dev

# Add regular dependencies here.
dependencies:
  args: ^2.2.2
  dio: ^5.3.2

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
